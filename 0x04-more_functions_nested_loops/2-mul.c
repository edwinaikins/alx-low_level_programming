#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: input character.
 * @b: input character.
 * Return: 0 success.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
